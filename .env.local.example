# MongoDB
MONGO_URI=mongodb://localhost:27017/nexus

# JWT
JWT_SECRET=change-me-to-a-random-string

# Next.js public env vars (injected at build time, exposed to client)
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_SOCKET_URL=
